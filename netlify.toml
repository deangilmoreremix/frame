[build]
  command = "pnpm install && pnpm --filter @frame/common run build && pnpm --filter @frame/backend run build && pnpm --filter @frame/web run build && pnpm run db:push"
  publish = "packages/frame-web/.next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_PUBLIC_API_URL = "/api"

[functions]
  directory = "packages/frame-backend/netlify/functions"
  node_bundler = "esbuild"
  included_files = ["packages/frame-backend/dist/**", "packages/frame-common/dist/**"]

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
